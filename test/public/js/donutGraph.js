$(document).ready(function () {
  const myChart = $("#first-graph").donutty({
    color: "#e0444c",
    min: 0,
    max: 100,
    value: 90,
    thickness: 5,
  });
});
$(document).ready(function () {
  const myChart = $("#second-graph").donutty({
    color: "#e0444c",
    min: 0,
    max: 100,
    value: 99,
    thickness: 5,
  });
});
$(document).ready(function () {
  const myChart = $("#third-graph").donutty({
    color: "#e0444c",
    min: 0,
    max: 100,
    value: 100,
    thickness: 5,
  });
});
